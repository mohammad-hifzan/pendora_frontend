console.log("Theme B JS loaded");

document.addEventListener('DOMContentLoaded', function () {
  // Initialize Bootstrap tooltips, sidebar toggle, or Gentelella widgets
  if (window.$) {
    $('[data-toggle="tooltip"]').tooltip();
  }

  // If you use plugins like Chart.js or Flot, init here
});
